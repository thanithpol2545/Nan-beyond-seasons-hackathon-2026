import raw from "../../nan_dataset.json";

export const FESTIVALS_DATA = raw.festivals || [];
export const FLOWERS_DATA = raw.flowers || [];
export const WELLNESS_COMMUNITIES = raw.wellness_communities || [];
